"0","```r
cv.errors = matrix(NA,10,28)
for(k in 1:10){
  best.fit=regsubsets(score~.,data = df_train[folds !=k,], nvmax = 28, method = \"forward\")
  for(i in 1:28){
    pred = predict(best.fit, df_train[folds == k,], id=i)
    cv.errors[k,i]= mean((df_train$score[folds==k]- pred)^2)
    
  }
  
}


mse.cv = (apply(cv.errors,2, mean))
plot(mse.cv,xlab = \"Number of variables\", ylab = \"Average MSE for all folds\", type=\"o\")

```"
